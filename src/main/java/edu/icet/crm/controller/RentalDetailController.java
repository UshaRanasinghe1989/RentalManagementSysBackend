/*package edu.icet.crm.controller;

import edu.icet.crm.model.RentalDetail;
import edu.icet.crm.service.RentalDetailService;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

@RestController
@CrossOrigin
@RequestMapping("/rental-detail")
@RequiredArgsConstructor
public class RentalDetailController {
    private final RentalDetailService service;
    @PostMapping()
    RentalDetail persist(@RequestBody RentalDetail rentalDetail){
        return service.persist(rentalDetail);
    }
}*/
